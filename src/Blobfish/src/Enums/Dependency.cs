namespace Blobfish
{
    /// <summary>
    /// Specified whether the Series is independent or dependent.
    /// </summary>
    public enum Dependency
    {
        /// <summary>
        /// Specifies that the Series is independent.
        /// </summary>
        Independent = 0,

        /// <summary>
        /// Specified that the Series is dependent.
        /// </summary>
        Dependent = 1,
    }
}
